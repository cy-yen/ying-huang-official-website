name: Deploy via ftp
on:
  push:
    branches: [ "master" ]
  workflow_dispatch:
jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Upload ftp
      uses: sebastianpopp/ftp-action@releases/v2
      with:
        host: ftp://18.166.80.41:21212
        user: webjimyftp
        password: webjimyftp@888
        localDir: "."
        remoteDir: "test"
        options: "--delete --asci"
